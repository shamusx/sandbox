apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: cni
  namespace: kube-system
spec:
  interval: 5m
  chart:
    spec:
      chart: cni
      version: 1.16.7
      sourceRef:
        kind: HelmRepository
        name: tetratelabs
        namespace: istio-system
      interval: 1m