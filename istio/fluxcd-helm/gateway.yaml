apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ingress-gateway
  namespace: istio-system
spec:
  interval: 5m
  chart:
    spec:
      chart: gateway
      version: 1.16.7
      sourceRef:
        kind: HelmRepository
        name: tetratelabs
        namespace: istio-system
      interval: 1m